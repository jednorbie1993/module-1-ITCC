#include <stdio.h>

int main() {
    int x = 10;

    x += 5;   // x = x + 5 → 15
    x *= 2;   // x = x * 2 → 30

    printf("x = %d\n", x);

    return 0;
}
//pababa kasi ang code so ung plus then time kaya naging = 30